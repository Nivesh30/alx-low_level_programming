#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers
 * @a: number one
 * @b: number two
 * Return: the multiplication of a and b
 */
int mul_int(int a, int b)
{
	return (a * b);
}
